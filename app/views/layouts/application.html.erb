<!DOCTYPE html>
<html>
<head>
  <title>Stwiki</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
<% if @current_user %>
   <div id="userinfo">
   	    <b><%= @current_user.name %></b>
		<%= link_to 'Logout', user_logout_path, :method => :post, :confirm => 'You sure?' %>
   </div>
<% end %>

<%= yield %>

</body>
</html>
